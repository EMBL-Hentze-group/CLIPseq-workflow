
bbduk {
    // ref = "/path/to/rRNA.fa"
    /*
    ref is a required parameter, but will be set by the genome profile.
    see directory conf/genome for available genome profiles
    */
    params = " "
}

