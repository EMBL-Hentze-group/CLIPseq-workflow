This directory contains configuration files for specific tools used in the Nextflow pipeline. 
Some tools require additional files/inputs such as genome indices, gene annotation files, etc with depend on the genome/organism. These parameters are set in the genome profiles in the directory `conf/genome`. 

There might be additional protocol specific parameter. These are set in the protocol profiles in the directory `conf/protocol`.